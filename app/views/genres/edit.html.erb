<h1>Editar gênero</h1>

<%= form_with model: @genre do |f| %>
  <%= f.label "Nome:" %>
  <%= f.text_field :name %>
  <%= f.submit "Salvar" %>
<% end %>

<%= render partial: 'shared/menu', locals: { link_to_previous_page: genres_path } %>